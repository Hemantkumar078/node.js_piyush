const math =require("./math.js");// require("pathname for importing from internal directory")
console.log(math.sub(2,5));
// common js module type for writing as require and type: "module" for writing it as import math from "./math.js"